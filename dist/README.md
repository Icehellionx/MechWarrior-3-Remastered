# Release output

`build.ps1` writes the setup executable and its SHA-256 digest here. The generated files are intentionally ignored by Git and should be uploaded together as assets on a tagged GitHub Release.
